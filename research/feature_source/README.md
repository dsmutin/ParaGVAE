# Feature source

Frozen VAE latent versus raw k-mer and depth. This is not end-to-end joint training of a VAE and a GCN.

Numbers in this folder predate the review fixes and are not valid until the rerun.

Strong100 is the only bundle whose two matrices differ. Other datasets store the same array in both roles, so the arms match.

Numbers and the chart: `benchmark/feature_source/`.
