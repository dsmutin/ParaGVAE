# Multiscale features

Latent features alone versus the same features plus degree and a local clustering coefficient. Self-loops are not counted as neighbours.

The structural pair lowers Strong100 (0.281 vs 0.312) and ONT 100M (0.248 vs 0.320), and raises ONT 1B (0.469 vs 0.354). Illumina moves from 0.165 to 0.188 at the epoch cap.

Numbers and the chart: `benchmark/multiscale/`.
