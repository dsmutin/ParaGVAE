# Benchmarks

Each folder is one hypothesis. `results.csv` is every seed. `summary.csv` is the mean. `f1.html` is the Altair chart.

The epoch loop is `cpp/gcn_train`. Inputs cached under `intermediates/` are MetaMetro CGT directories plus the feature matrices the arms reuse.

See `docs/findings.md` for the comparison.
